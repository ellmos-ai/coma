# MARKETING-LOG — COMA (Command & Communication for Autonomous Agents)
# Stand: 2026-09-14 | Repository: ellmos-ai/coma | Lizenz: MIT

## 1. Positionierung & Kernnutzen (Value Proposition)

COMA (`coma`) ist die schlanke, dateibasierte Lebenszyklus- und Entkopplungsschicht für autonome KI-Agenten.
Es löst das fundamentale Problem interaktiver Agenten-Sessions (wie z. B. Claude Code CLI, OpenAI Codex CLI, Google Antigravity / AGY CLI und Kimi Code CLI):
Wenn Agenten in Remote-Control- oder interaktiven Terminalsitzungen ausgeführt werden, blockieren Berechtigungsabfragen
(`--dangerously-skip-permissions`, `dontAsk` etc.) häufig bei unbesetzter Konsole oder hinterlassen hängende Prozesse.

COMA startet den Agenten als eigenständigen, entkoppelten Betriebssystem-Prozess außerhalb der interaktiven Session und
etabliert einen kollisionsfreien Dateisystem-Kanal (`IN/`, `OUT/`, `DONE/`).

- **Zero-Dependency:** 100% Python Standard-Bibliothek, null externe Pakete zur Laufzeit.
- **Zero-Network-Egress:** Keine Netzwerkverbindungen, keine Cloud-Dienste, keine Kontopflicht, 100% lokal.
- **Single-Writer-Protokoll:** Jede Datei im Auftragsbaum besitzt genau einen autorisierten Schreiber — strukturelle Vermeidung von Race Conditions und Lock-Konflikten.
- **Prozessfreie Kommandokonstruktion:** `--dry-run`, `cmd` und `build_session_plan()` erzeugen verifizierte argv-Vektoren ohne Prozessstart und ohne Tokenverbrauch.

---

## 2. Ziel-Personas (Target Audience)

1. **Autonomous Agent Architects & Framework Developers**
   - Entwickler, die Multi-Agenten-Pipelines und Swarm-Strukturen aufsetzen.
   - Suchen eine robuste, dateibasierte Entkopplung zwischen Orchestrator-Session und ausführendem Worker-Prozess.

2. **Local-First & Privacy-Conscious AI Engineers**
   - Teams mit strikten Air-Gapped-, DSGVO- oder On-Prem-Vorgaben, die keine externen Daemon-Dienste oder Cloud-Orchestratoren zulassen.
   - Profitieren vom garantierten Zero-Network-Egress und auditierbaren Dateisystem-Kanälen.

3. **Claude Code / Codex / Antigravity CLI Power Users**
   - Anwender, die Hintergrund-Jobs unbeaufsichtigt und headless ablaufen lassen möchten, ohne dass interaktive Prompts den Ablauf blockieren.
   - Nutzen vorgefertigte CLI-Adapter (`ClaudeAdapter`, `CodexAdapter`, `AntigravityAdapter`, `KimiAdapter`).

4. **Edge & Headless Automation Operators**
   - Betreiber von Scheduled Tasks, Cronjobs oder lokalen Service-Pipelines, die strukturierte Statusmeldungen (`coma.<jobid>.json`), Konsolen-Logs (`console.log`) und Fortschritts-Streams (`from-agent.jsonl`) benötigen.

---

## 3. Discoverability & SEO-Keywords

`autonomous-agents`, `agent-lifecycle`, `subprocess-spawner`, `claude-code`, `codex-cli`, `antigravity-cli`, `kimi-cli`,
`multi-agent`, `session-decoupling`, `job-board`, `file-based-queue`, `local-first`, `zero-dependency`, `zero-egress`,
`process-isolation`, `python-stdlib`, `ellmos-ai`, `open-bricks`

---

## 4. Verzeichnis- & Katalog-Empfehlungen (Directory Submissions)

Folgende Verzeichnisse und Community-Sammlungen eignen sich für eine Aufnahme von COMA:

| Verzeichnis / Liste | Kategorie | Begründung / Relevanz |
|---|---|---|
| **Awesome-AI-Agents** (`e2b-dev/awesome-ai-agents`) | Agent Infrastructure / Tools | Etabliertes Referenzverzeichnis für Agenten-Entwicklung; COMA füllt die Lücke des entkoppelten Subprozess-Spawners. |
| **Awesome-Local-AI** | Local Infrastructure | Zeigt lokale, serverlose Orchestrierung ohne externe Cloud-Abhängigkeiten. |
| **Awesome-Claude** | Developer Tools / Extensions | Direkter Support für Claude Code CLI via `ClaudeAdapter` mit Berechtigungs-Flag-Übersetzung. |
| **LibHunt / Python** | Task Queue / Process Automation | Listet Python-Pakete; COMA bietet Zero-Dependency JobBoard-Alternative zu Celery/RQ für lokale Agenten. |
| **PyPI (Python Package Index)** | Package Release | Bereitstellung als `pip install coma-agents` oder `ellmos-coma` nach Erreichen von v0.3.x/v1.0.0. |

---

## 5. Ökosystem-Integration & Geschwister-Werkzeuge

COMA ist integraler Bestandteil der `ellmos-ai` und `open-bricks` Infrastruktur:

- **`ellmos-ai/policy-registry`**: Liefert Governance-Regeln und Rollendefinitionen, die als Prompts über `IN/<jobid>.md` übergeben werden.
- **`ellmos-ai/lock-master`**: Verwaltet Projekt- und Dateisperren (Roshambo-Schicht) getrennt vom Prozesslebenszyklus in COMA.
- **`ellmos-ai/rinnsal`**: Nutzt COMA als lokalen Ausführungskanal für autonome Hintergrundaufgaben und SQLite-gepufferte Kontextübergabe.
- **`ellmos-ai/condition-gates`**: Validiert Vorbedingungen und Evidenzen, bevor ein COMA-Job gestartet oder als fertig markiert wird.
- **`ellmos-ai/workflowhooker`**: Löst COMA-Job-Starts ereignisgesteuert über Dateisystem- oder Webhook-Signale aus.
- **`ellmos-ai/memoryhooker`**: Sichert Erfahrungen und Lessons Learned nach erfolgreichem Abschluss eines COMA-Jobs.
- **`dev-bricks/safe-start-for-codex`**: Führt Umgebungs- und Health-Checks vor dem Start von COMA-Agenten durch.

---

## 6. Konkrete Marketing- & Design-Empfehlungen (Roadmap)

1. **Asciinema Terminal Screencast:**
   - Aufnahme eines kurzen (30–45s) Terminal-Demos: `coma submit`, `coma run --dry-run`, `coma run`, `coma status` und `coma result`.
   - Einbindung als SVG/GIF in `README.md` und `README_de.md`.

2. **PyPI Package Publication (`coma-agents`):**
   - Sobald das Modul-Gate freigegeben ist, Paketierung über `build` und Veröffentlichung auf PyPI.
   - Ermöglicht `pip install coma-agents` für weltweite Nutzbarkeit.

3. **Multi-Agent MCP Bridge:**
   - Bereitstellung eines schlanken MCP-Tools (`coma_submit`, `coma_poll`, `coma_result`), damit Claude Desktop, Cursor und Antigravity COMA-Hintergrundjobs nativ ansteuern können.

4. **Dual-Language Interaktive Quickstarts:**
   - Ausbau von interaktiven Python-Notebooks / Beispielen unter `examples/` für Standard-Agent-Workflows (Refactoring, Code-Review, Test-Generierung).

---

## 7. Release 0.3.1 Highlights (Pfad A — Quality, Stale Automation & Multi-Host Defense Audit 2026-09-14)

- **CI Workflow & Concurrency Hardening:** Deployed `.github/workflows/tests.yml` with multi-OS (Ubuntu, Windows, macOS) and multi-Python (3.10–3.13) matrix, `timeout-minutes: 15` runaway guardrail, and `cancel-in-progress: true` concurrency group cancellation.
- **Stale Issues & PRs Lifecycle Automation:** Added `.github/workflows/stale.yml` using `actions/stale@v9` with `timeout-minutes: 10`, daily scheduled cron (`30 1 * * *`), and least-privilege permissions (`issues: write`, `pull-requests: write`).
- **Welcome Workflow Deployment:** Added `.github/workflows/welcome.yml` with `timeout-minutes: 5` execution bounds and welcoming guidance for community issues and PRs.
- **Multi-Host & Conflict Hardening:** Extended `.gitignore` against cross-system sync artifacts across multi-host environments (`* (Kopie)*`, `* (Copy)*`, `*conflicted copy*`, `*-WORKSTATION*`, `*-ASUS*`, `*-LAPTOP*`, `*-Mac Studio*`), canonical lock indicators (`LOCK`, `LOCK.*`, `uv.lock`), and coverage/cache directories.
- **PEP 621 Standard Compliance:** Registered `"LLM Ready"` context URL pointing to `llms.txt` and configured standard Pytest options (`addopts = "-ra -v"`) in `pyproject.toml`.
- **Version Harmonization:** Synchronized release version to `0.3.1` across package manifests, module definition, and documentation.
- **Automated Contract Tests:** Expanded `tests/test_metadata.py` with contract tests verifying CI timeouts, stale workflow configuration, multi-host gitignore defense, PEP 621 metadata, and marketing log recency.

---

## 8. Release 0.3.2 Highlights (Pfad B — Marketing, Discoverability, Visual Architecture & SBOM Re-Certification 2026-09-19)

- **18-Point Quick Navigation Parity:** Introduced identical 18-point numbered navigation structures in both `README.md` and `README_de.md` with 100% reciprocal HTML anchors (`<a id="..."></a>`).
- **Target Personas & Search Intent Mapping:** Formulated 4 dedicated personas (`[PERSONA-01]` Autonomous Agent Swarm Architects, `[PERSONA-02]` Local-First Sovereign Developers, `[PERSONA-03]` Remote-Control Headless DevOps, `[PERSONA-04]` Enterprise Security Auditors) and mapped high-intent search queries in English and German.
- **10-Dimension Comparative Matrix:** Benchmarked COMA against Celery/RQ, Temporal/Camunda, raw Python subprocesses, and cloud agent frameworks across 10 architectural invariants (`INV-COMA-01` to `INV-COMA-10`).
- **Dual Mermaid Visualizations:** Added 5-layer system architecture topology flowchart (`flowchart TD`) and sequence diagram (`sequenceDiagram`) with zero trailing semicolons and strict label escaping.
- **Level 1 SBOM & Unprivileged Certification:** Updated `THIRD_PARTY_LICENSES.md` to Stand 2026-09-19 with complete SPDX package inventory, 10-invariant compliance matrix, and `RunAsInvoker` non-elevation certification.
- **Statutory Notice (§ 521 BGB):** Embedded statutory liability limitation notice for gratuitous open-source software under German Law.
- **GitHub Discoverability:** Expanded topics to full 20/20 capacity (`agent-lifecycle`, `agent-orchestration`, `ai-agents`, `antigravity`, `claude-code`, `cli`, `codex`, `job-board`, `local-first`, `multi-agent`, `offline-first`, `open-bricks`, `process-isolation`, `python`, `session-decoupling`, `spawn`, `status-polling`, `subagents`, `zero-dependencies`, `zero-egress`) and verified homepage URL via GitHub CLI.
- **Contract Test Suite Expansion:** Added automated tests in `tests/test_metadata.py` verifying navigation parity, persona definitions, comparative matrix, § 521 BGB disclaimer, and SBOM recency (284 passed tests | 100% green).
